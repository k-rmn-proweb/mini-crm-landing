import { Section, SectionHeader } from "@/components/ui/section";

export default function HomePage() {
  // Placeholder band — replaced by the real sections in the next phases.
  return (
    <Section>
      <SectionHeader
        eyebrow="Phase 1"
        title="Design system and layout are in place"
        description="Tokens, typography, container rhythm, header and footer are wired up. Sections land next."
      />
    </Section>
  );
}
